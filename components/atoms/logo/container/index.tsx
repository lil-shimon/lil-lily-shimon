import { LogoPresenter } from '../presenter';

/**
 * LogoContainer
 * @constructor
 */
export const LogoContainer = () => {

  /// TODO: impl logo
  const imgSrc = '';
  return <LogoPresenter imgSrc={imgSrc} />;
};